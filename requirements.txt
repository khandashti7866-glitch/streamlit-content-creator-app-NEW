# Core
streamlit>=1.20
pandas
numpy

# Optional (only needed if you want transformer model generation)
torch
transformers

# Optional for image handling / Pillow references (not strictly required)
Pillow


